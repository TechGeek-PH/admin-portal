plugins {
    id 'com.android.application'
}

android {
    namespace 'ph.techgeek.installer'
    compileSdk 35

    defaultConfig {
        applicationId 'ph.techgeek.app'
        minSdk 24
        targetSdk 35
        versionCode 10
        versionName '1.3.1'
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
}
