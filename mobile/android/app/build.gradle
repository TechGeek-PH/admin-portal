plugins {
    id 'com.android.application'
}

android {
    namespace 'ph.techgeek.installer'
    compileSdk 35

    defaultConfig {
        applicationId 'ph.techgeek.installer'
        minSdk 24
        targetSdk 35
        versionCode 4
        versionName '1.0.3'
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
}
